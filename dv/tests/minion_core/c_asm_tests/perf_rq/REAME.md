- Thread0 and Thread1 running different combinations of sequences dealing with the DCACHE replay queue.
- Once each sub test finishes, it outputs the total time it took (via et_print)

- This test is supposed to be run in different branches to compare times (e.g. A0 vs PTR vs GPD)
