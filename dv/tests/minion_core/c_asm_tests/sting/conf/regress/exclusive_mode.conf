import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"

# Force test to run in machine mode to prevent timeout
PRIV_LEVEL.USER = 0
PRIV_LEVEL.SUPERVISOR = 0

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.OPGRP = CPU[0].OP_GROUPS.OPGRP + {
                SNP_exclusive_mode: 150
               }


