import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"

# Replace the snippet pool with one that contains only exceptions
# The instruction pool is left untouched and contains a wide array of possible instructions
CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.RAND_SNIPPET = { SNP_illegal_insn : 1}

