#ETDV_SEED_MULTIPLIER = 0

# This config contains the newest snippets that are under testing, but
# that we plan to add into Sting Rand. As there might be potential incompatibilities
# with the snippets that are already in Sting Rand, we only add the newest ones
# 1/3 of the times to avoid 0% of pass-rate

RAND_ENV.TESTING_MIN_BIAS = 10

if ET_TEST_ENV.EOT_ZEBU_HINTS == 0:

   RAND_ENV.INCLUDE_LATEST = @cpa_urandrange(0,3)
   if RAND_ENV.INCLUDE_LATEST == 0:

      # SCP OFF / NO TENSOR OPS
      if ET_TEST_ENV.USE_TENSOR_SCP_OPS == 0:
         ET_SNIPPET_GRP = ET_SNIPPET_GRP + {
            # This snippet cannot be set if cooperative snippets are used
            # as cooperative snippets might work for one Minion and fail for another, causing hangs.
            SNP_cache_control_except          : @cpa_urandrange(RAND_ENV.TESTING_MIN_BIAS,20)
         }
      endif

      if ((ET_L2SCP.L2SCP_IN_MEM_SET == 1) and (ET_MEM_ALIAS.L2SCP == 0)):
         ET_SNIPPET_GRP = ET_SNIPPET_GRP + {
            #SNP_basic_mem_access               : @cpa_urandrange(2,20)
            #SNP_atomic_mem_access              : @cpa_urandrange(2,20)
            #SNP_mem_access_fault               : @cpa_urandrange(2,20)
         }
      endif

      ET_SNIPPET_GRP = ET_SNIPPET_GRP + {
         #SNP_ps_pi_pma_ok                   : @cpa_urandrange(2,20)
         #SNP_ps_pi_pma_err                  : @cpa_urandrange(2,20)
         #SNP_ifetch_pma                     : @cpa_urandrange(2,20)
         #SNP_cacheops_cacheable_pma         : @cpa_urandrange(2,20)
         #SNP_cacheops_NoNcacheable_pma      : @cpa_urandrange(2,20)
         #SNP_regular_ldst_pma_ok            : @cpa_urandrange(2,20)
         #SNP_regular_ldst_pma_err           : @cpa_urandrange(2,20)
         #SNP_regular_ldst_pma_mprot         : @cpa_urandrange(2,20)

         #SNP_fcc_threads_sync               : @cpa_urandrange(2,20)
         #SNP_fcc_wait_for_master            : @cpa_urandrange(2,20)
         #SNP_fcc_overflow                   : @cpa_urandrange(2,20)

         #SNP_tensor_load_pma_mprot        : @cpa_urandrange(TENSOR_ENV.TEN_MIN_BIAS,TENSOR_ENV.TEN_MAX_BIAS)
         #SNP_tensor_load_pma_ok           : @cpa_urandrange(TENSOR_ENV.TEN_MIN_BIAS,TENSOR_ENV.TEN_MAX_BIAS)
         #SNP_tensor_store_pma_error       : @cpa_urandrange(TENSOR_ENV.TEN_MIN_BIAS,TENSOR_ENV.TEN_MAX_BIAS)
         #SNP_tensor_store_pma_mprot       : @cpa_urandrange(TENSOR_ENV.TEN_MIN_BIAS,TENSOR_ENV.TEN_MAX_BIAS)
         #SNP_tensor_store_pma_ok          : @cpa_urandrange(TENSOR_ENV.TEN_MIN_BIAS,TENSOR_ENV.TEN_MAX_BIAS)
      }

   endif

endif
