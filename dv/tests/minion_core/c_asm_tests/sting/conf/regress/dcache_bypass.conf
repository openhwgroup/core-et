#ETDV_DEV
# Disable this config at this point as sting kernel needs unaligned accesses
# and these cause traps in bypass mode then test failures
#ETDV_SEED_MULTIPLIER = 0
#
#Use Shire build as need to access ESR
#ETDV_BUILD = 'shire'
import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"

RV_PARAMS.HAS_UNALIGNED = 0

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.TEST_START = CPU[0].OP_GROUPS.TEST_START + [
                                                         { SNP_set_dcache_bypass : 1 }
                                                  ]

