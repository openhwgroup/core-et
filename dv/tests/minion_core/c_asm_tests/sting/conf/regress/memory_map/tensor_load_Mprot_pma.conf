import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"
import "$MINION_DIAGS/sting/conf/regress/tensors/tensor_load.conf"


#ETDV_BUILD = 'shire'

CPU[0-BOARD_CONF.NUM_CPUS].TEST_PROPS.NUM_OPS = 250
ET_TEST_ENV.SCP_ENABLE = 1

# RV_TLD contains valid tensor load accesses
# So we mix valid tensor_loads with other tloads that access other mem regions
RV_TLD_PMA = RV_TLD + {
   SNP_tensor_load_pma_mprot   : 20
}

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.OPGRP = RV_TLD_PMA

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.TEST_START = CPU[0].OP_GROUPS.TEST_START + [
                                                       { SNP_enable_l1scp : 1 }
                                                  ]

#CPU[0-BOARD_CONF.NUM_CPUS].TEST_PROPS.NUM_OPS = 10000
