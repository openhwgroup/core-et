#ETDV_DEV
import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"
import "$MINION_DIAGS/sting/conf/regress/only_ldst.conf"

#ETDV_MIN_HARTS = 10000

RV_TRAP_OVER_TRAP_IFETCH_GRP = {
    SNP_trap_over_trap_ifetch_pma : 100
}

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.OPGRP = RV_TRAP_OVER_TRAP_IFETCH_GRP
CPU[0-BOARD_CONF.NUM_CPUS].TEST_PROPS.NUM_OPS = 100
