import "$STING_ROOT/regress.d/confs/riscv/regress_base.conf"
import "$MINION_DIAGS/sting/conf/utils/et_minion_shire_mem.conf"
import "$MINION_DIAGS/sting/conf/regress/only_ldst.conf"

ET_TEST_ENV.SCP_ENABLE = 1

RV_TLD = RV_LDST_GRP + {
   SNP_tensor_load_cov       : 50
   SNP_tensor_load_b_cov     : 38
   SNP_tensor_load_coop_cov  : 98
   SNP_tensor_load_pma_error : 14
}

# Tensor Load L2
if (ET_L2SCP.INCL_TL_L2SCP_OPS == 1):
   RV_TLD = RV_LDST_GRP + {
      SNP_tensor_load_l2scp_cov : 26
   }
endif

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.TEST_START = CPU[0].OP_GROUPS.TEST_START + [
                                                       { SNP_enable_coop_mode : 1 }
                                                  ]

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.OPGRP = RV_TLD

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.TEST_START = CPU[0].OP_GROUPS.TEST_START + [
                                                       { SNP_enable_l1scp : 1 }
                                                  ]

CPU[0-BOARD_CONF.NUM_CPUS].OP_GROUPS.TEST_START = CPU[0].OP_GROUPS.TEST_START + [
                                                       { SNP_clear_tenc : 1 }
                                                  ]
