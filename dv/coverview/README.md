# Coverage Dashboard

Interactive coverage dashboard using [coverview](https://github.com/antmicro/coverview) (Apache 2.0).

`index.html` and `embed.py` are from the coverview project.

## Usage

```bash
# From the repo root, after running tests:
make coverage-html    # generates build/coverview/index.html
open build/coverview/index.html
```
